"use strict"

// Крок 0. Позначення даних
// Крок 1. Введення даних:
// Крок 2. Обчислення
// Крок 3. виведення результату


// Задача №1 Вивести на екран номери місяців весни і літа (від 3 до 8)

if (confirm('Почати тестування?')) {

	for (let i = 3; i <= 8; i++) {
		if (i <= 5)
			document.write(`${i} місяць - весна <br>`)
		else
			document.write(`${i} місяць - літо <br>`)
	}

}
